mopl:
  cache:
    key-prefix: "mopl:"
    invalidation-channel: "cache:invalidation"
    redis-enabled: ${CACHE_REDIS_ENABLED:false}
    l1:
      maximum-size: 10000
      ttl: 1m
      record-stats: true
    l2:
      default-ttl: 10m
