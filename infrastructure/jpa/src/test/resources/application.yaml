# =============================================================================
# JPA Module Test Configuration
# =============================================================================
# JPA 모듈 단독 테스트를 위한 최소 설정입니다.
# test 프로필이 활성화되면 jpa.yaml의 test 프로필 설정이 적용됩니다.
# =============================================================================

spring:
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false
  sql:
    init:
      mode: never
